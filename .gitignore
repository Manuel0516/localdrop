# Local config holds the shared token -- never commit it.
config.toml

# LocalDrop service files
com.localdrop.plist 
localdrop.service

# Python
__pycache__/
*.pyc

# Received files (if someone points download_dir inside the repo)
Downloads/
LocalDrop/
